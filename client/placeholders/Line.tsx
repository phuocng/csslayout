import React from 'react';

const Line: React.FC<{}> = () => {
    return (
        <div
            style={{
                backgroundColor: 'rgba(0, 0, 0, 0.3)',
                height: '1px',
                width: '100%',
            }}
        />
    );
};

export default Line;
